# current-index.patch — git diff --cached --binary at audit freeze (2026-07-24)
# Result: nothing staged (empty diff).
# --- begin git diff --cached --binary ---
# --- end (no hunks: index clean) ---
